# Nested Agents Example

This example is a runnable program that reads:

`examples/nested-agents/file/path/example.txt`

and shows the exact `fs_read` tool result with hierarchical `AGENTS.md` reminders appended.

## Run

```bash
pnpm example:nested-agents
```
